# mneme-cli

CLI tool for mneme-memory.

## Commands

```bash
mneme init       # Initialize mneme-memory (detect tier, create config)
mneme retain     # Store a memory from stdin
mneme recall     # Search memories
mneme serve      # Start the API server
mneme status     # Show current status and tier
```
